# ============================================================================
# AgentIM — Environment Variables
#
# Only infrastructure settings that REQUIRE a server restart are listed here.
# All other settings (OAuth, S3, rate limits, AI router, cleanup, etc.) are
# runtime-configurable via the Admin Settings web UI — no restart needed.
# ============================================================================

# Server
PORT=3000
HOST=0.0.0.0
# NODE_ENV=production

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/agentim

# Redis (optional — omit for single-node deployments)
# REDIS_URL=redis://localhost:6379

# Auto-run database migrations on startup (set "false" for multi-replica deploys)
# RUN_MIGRATIONS=true

# JWT secret (REQUIRED in production: generate with `openssl rand -hex 32`)
JWT_SECRET=change-me-to-a-random-secret
# JWT_SECRET_PREVIOUS=          # Previous secret for graceful key rotation

# Admin credentials (seeded on startup)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=ChangeMe123!

# File uploads (local storage path)
UPLOAD_DIR=./uploads

# CORS origin (REQUIRED in production — server refuses to start without it)
# Auto-detected from RENDER_EXTERNAL_URL / RAILWAY_PUBLIC_DOMAIN if not set.
# Runtime-configurable via Admin Settings after initial startup.
CORS_ORIGIN=http://localhost:5173

# Encryption key for secrets at rest (REQUIRED in production, min 32 chars)
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=
